# 핸드오버: 인공지능 사전역량 학습자료 3권 + 합본 제작 (hwpx → hwp)

**날짜**: 2026-09-25
**이전 핸드오버**: handover-20260925-hatsoja-member-roles.md (같은 날 병행 세션)
**작업 폴더**: `D:\사교원 개발그룹\망남마을학교 시범사업\` (산출물) / `…\build\` (생성 스크립트)

---

## 수행한 작업

### 1. 사전진단 10문항 채점 · 해설

교육 「[사전역량] 1019-1119 기업형 sLLM기반 신재생에너지 AI 에이전트 개발」의 진단 10문항
(이미지 4장)을 읽고 정답과 근거를 제시. 정답: ③②③①①②③④②②.

함정이 있던 문항 — 1번 ④는 "플랫폼 독립적이다"와 "XML은 확장 가능한 마크업 언어이다"가
한 보기에 붙어 있으나 답은 ③(컴파일러 기본 제공). 7번은 은닉층 증가가 항상 정확도를
올리지는 않지만 보기 중 장점은 ③뿐.

### 2. 학습자료 3권 제작 (hwpx 경로 D)

서식 템플릿은 `연대지능활동가 아카데미\tpl_all12.hwpx` (본문·셀 12pt)를 재사용.

| 자료 | 제목 | 쪽 | 표 |
|---|---|---|---|
| ① | 인공지능 사전역량 학습자료 (10문항 기초, 9장) | 11 | 29 |
| ② | 심화 ② 데이터에서 모델까지 (딥러닝 본체, 11장) | 10 | 30 |
| ③ | 심화 ③ 기업형 sLLM과 AI 에이전트 (10장) | 10 | 31 |

- ①은 초안 14쪽 → 장마다 걸린 `page_break=True`를 걷어내 11쪽. 1~10쪽 여백 8%로 포화,
  11쪽만 59% — 남은 1쪽은 조판 낭비가 아니라 내용 분량임을 확인해 보고.
- ②는 초안 8쪽으로 얇아 실무 절 4개(학습 실패 점검 10항목 · CNN 모델 계보 ·
  BERT/GPT 비교 · 하이퍼파라미터 튜닝과 재현성)를 추가해 10쪽으로 맞춤.
- ③에서 `O&M` 표기를 소스에 `O&amp;M`으로 적어 PDF에 `O&amp;M`으로 찍히는 이중
  이스케이프 오류 발견 → 소스를 `O&M`으로 고쳐 재생성.

### 3. 합본 제작 (새 기법)

`build_ai_combined.py` — 낱권 스크립트의 **본문 구간만 떼어 내 하나의 doc에 exec**한다.
내용을 복사하지 않으므로 낱권을 고치면 합본은 재실행만으로 반영된다.

- 32쪽 / 표 93개 (29+30+31+통합본 앞표 3).
- 앞에 통합 목차 3표 추가: 부별 구성 · 상황별 읽는 순서 5가지 · 세 부의 관계 대조표.
- `title()`은 secPr을 운반하므로 통합본에서 한 번만 호출하고, 각 부 제목은
  쪽 나눔 + 제목 서식 문단(`PT()`)으로 따로 냈다.

### 4. .hwp 변환 · 검증

네 문서 모두 `run_auto.ps1` + `hwpx_to_hwp.ps1`로 구형 .hwp 생성 후, `hwp_verify.ps1`로
**다시 열어** 쪽수와 장 제목·용어사전·셀프체크가 온전한지 확인. 전부 쪽수 일치.

### 5. 스킬 갱신

`hwpx-powershell-edit`에 **경로 I(낱권 합본)** 와 함정 4건 추가. (아래 "파일 위치")

## 미완료 / 다음 할 일

- [ ] 사용자 판단 대기: 낱권 9개 파일을 남길지, 합본만 남기고 정리할지
- [ ] 자료 ①을 정확히 10쪽으로 줄일지 여부 (줄이려면 1-3 · 4-3 · 7-3 표 중 하나를 덜어야 함)
- [ ] 교육 수강 후 실제 커리큘럼과 대조해 자료 ③의 도입 로드맵 10단계 보정

## 파일 위치

| 경로 | 내용 |
|---|---|
| `D:\사교원 개발그룹\망남마을학교 시범사업\인공지능_사전역량_학습자료_20260925.{hwp,hwpx,pdf}` | 자료 ① 11쪽 |
| `…\인공지능_심화학습자료2_딥러닝실전_20260925.{hwp,hwpx,pdf}` | 자료 ② 10쪽 |
| `…\인공지능_심화학습자료3_sLLM에이전트_20260925.{hwp,hwpx,pdf}` | 자료 ③ 10쪽 |
| `…\인공지능_학습자료_통합본_20260925.{hwp,hwpx,pdf}` | 합본 32쪽 |
| `…\build\build_ai_prestudy.py` · `build_ai_study2.py` · `build_ai_study3.py` | 낱권 생성기 |
| `…\build\build_ai_combined.py` | 합본 생성기 (낱권 본문을 exec) |
| `…\build\tpl_all12.hwpx` | 서식 템플릿 사본 |
| `C:\Users\User\sakyowon-ai\skills\hwpx-powershell-edit\SKILL.md` | 경로 I · 함정 추가 |

수정은 반드시 **build 스크립트 → 재실행** 순서로 한다. .hwp를 직접 고치면 네 판본이 갈라진다.

## 약속

- 사용자가 **"이어서 작업하자"** 라고 하면 이 핸드오버의 "미완료" 목록부터 재개
- 사용자가 **"정리해"** 라고 하면: ①핸드오버 ②스킬 ③레슨 ④위키배포 자동 수행
