# 핸드오버: 위키 저장처 정정(사교원 위키) · 배포 사슬 규명 · DSO 문서 12pt 재생성

**날짜**: 2026-09-11 (정리 2026-09-16)
**이전 핸드오버**: handover-20260910-energy-pilot-200mw-dso.md
**작업 폴더**: `D:\사교원 개발그룹\전남광주사업게획서 모음\`

---

## 수행한 작업

### 1. 위키 404의 진짜 원인 — 배포 장애가 아니라 잘못된 완료 정의

2026-08~09 내내 "공동위키 머지 → wiki.poomasi.org 404 → 배포 대기 이월"이 반복돼 대기가 9건까지 쌓였다. 원인을 파고든 결과 **문제 자체가 존재하지 않았다.**

| 확인 항목 | 결과 |
|---|---|
| `wiki.poomasi.org` | **지미 개인 학습 위키**. 섹션이 품아이 · 지미 · 거버넌스 · 데이터 · 인프라이고 `/실천기술/` 경로가 없다 |
| 공동위키 레포 메타 | `homepageUrl` 없음, Actions 0개, **Pages 미설정**(API 404) |
| 공동위키 README | "공동 편집자가 머지하면 공동위키에 반영됩니다" — **머지가 곧 공개** |
| 실제 문서 접근 | GitHub 레포 URL에서 정상 렌더 확인(제목·본문·저자까지) |

**문서 14건은 처음부터 공개돼 있었다.** 존재하지 않는 배포 파이프라인을 기다린 것이다. 공동위키의 완료 정의를 **"PR 머지"** 로 고쳤다.

### 2. 사교원 위키로 저장처 변경 + 14건 이전

사용자 지시로 **앞으로 위키 저장은 사교원 위키가 기본**이 됐고, 기존 14건도 옮겼다(공동위키 원본은 그대로 유지).

- 대상: 공동위키 `문서/실천기술/`에서 deka2026이 등록한 14건(타인 기여 3건 제외)
- 분류: 주제별 분산 — 자산기반-사회연대경제 5 · 연대지능 4 · 에너지-전환 2 · 망남-신활력 2 · 연대지능아카데미 1
- frontmatter를 Quartz 관행(title/date/author/tags 리스트형)으로 변환, 본문 끝에 공동위키 원본 링크를 남겨 두 사본이 말없이 어긋나지 않게 함
- 커밋: `deka2026/sakyowon-wiki` v4 **fd6f043**

### 3. 사교원 위키 배포 사슬 규명 — 4단계이고 2단계만 자동이다

push 후에도 라이브가 바뀌지 않아 헤더까지 확인한 결과 **`Server: Caddy`** — GitHub Pages가 아니라 가비아 자체서버가 서빙하고 있었다.

| 단계 | 내용 | 자동 | 이번 상태 |
|---|---|---|---|
| 1 | `sakyowon-wiki` v4 `content/<분류>/` push | 수동 | ✅ `fd6f043` |
| 2 | `Deploy Quartz to GitHub Pages` 빌드 → `github-pages` 아티팩트 | **자동** | ✅ 입력 79→**93**, sitemap 72→**93** |
| 3 | 아티팩트를 `sakyowon-wiki-site` master에 반영 | 수동 | ✅ `07c2098` (내가 대신 수행) |
| 4 | 서버에서 `server/deploy-www.sh` 실행 | **수동·SSH** | ❌ **미실행 — 내가 못 함** |

3단계는 로컬 재빌드 대신 `gh run download`로 CI 산출물을 그대로 올렸다(내용이 CI와 100% 동일해 오차 없음).

`server/deploy-www.sh`가 `sakyowon-wiki-site` master를 클론해 `/opt/sakyowon/www/sakyowon-wiki`에 배치하는 구조이고, Caddyfile 주석에 "연대지능 공동위키(wiki.poomasi.org)를 제외한 모든 사이트를 이 서버에서 서빙한다"고 명시돼 있어 두 위키의 관계도 확정됐다.

### 4. DSO 문서 제2판 — 별도 문서 확정 + 표 셀 12pt

- 사용자가 **별도 문서 유지**로 결정(계획서 5-2 절로 합치지 않음)
- 표 셀까지 12pt로 재생성, 쪽 나눔 2개 제거해 빈 공간 정리 → **6쪽**
- 산출: `배전계통_운영권한과_읍면단위_DSO_20260910_v2.hwpx` (+PDF)

### 5. 표 셀 12pt 규칙 확정

이전의 "본문 12pt / 표 셀 10pt 유지" 관행을 **폐기**하고 본문·표 셀 모두 12pt로 확정(사용자 지시). 스킬에 `patch_body_12pt.py`를 두어 원본 charPr을 건드리지 않고 12pt 사본을 추가하는 방식으로 적용한다.

## 미완료 / 다음 할 일

- [ ] **사교원 위키 4단계 — 서버에서 `deploy-www.sh` 실행** (SSH 필요). 2026-09-16 확인 시 라이브는 여전히 09-05 빌드(sitemap 79 URL), 이전한 14건 전부 404
- [ ] (이월) DSO 문서와 계획서의 관계 — 별도 유지로 확정됐으나, 계획서 5장에 DSO 절 요약을 한 단락 넣을지는 미정
- [ ] (이월) 확인 필요 자료 5종 — 대단위간척 편입토지 세목고시 / RAWRIS 저수지 시설제원(고천암호·장성호) / 공영주차장 주차구획 면적 / 전남 특화지역 세부 운영계획 원문 / 구역전기사업 허가 기준
- [ ] (이월) 한전 분산형전원 연계 기술기준 최신본 — 일반선로/전용선로 임계값 원문 확인(1,000kW는 실무 통용 수치로 미확정)
- [ ] (이월) 표면적 산정 문서의 장성군 판정 — 장성호 만수면적 확보 후 확정

## 정리해 루틴 결과 (2026-09-16)

| 단계 | 결과 |
|---|---|
| ① 핸드오버 | 본 파일. sakyowon-ai master `2cbdcec` |
| ② 스킬 | `jeongrihae-routine` — 라이브 확인을 `curl -I` + `sitemap.xml` `<loc>` 개수 + 상태코드 3종으로 명문화. **WebFetch로 개수를 물으면 어림값이 나온다**(79를 72로 답한 사례)는 함정 추가. 같은 커밋 |
| ③ 레슨 | `content/연대지능/배포했는데-라이브가-아니다-사슬을-끝까지-확인하는-법.md` — 사교원 위키 v4 `7e92c0e` |
| ④ 위키배포 | **3단계까지 완료** — 1단계 `7e92c0e` / 2단계 CI run 35070484890 성공(문서 100건, 350파일) / 3단계 배포 레포에 반영 확인(다른 세션이 같은 CI 산출물로 push, 내 레슨 포함). **4단계 서버 반영 미실행** |

**2026-09-16 라이브 확인 결과** — `Last-Modified: Sun, 06 Sep 2026`, sitemap `<loc>` **79**(빌드는 100), 오늘 레슨 **404**. 09-11 이전분 14건도 여전히 404. 서버에서 `deploy-www.sh`가 한 번도 실행되지 않았다.

## 파일 위치

| 경로 | 내용 |
|---|---|
| `...\세부사업 계획서\3_..._20260911.hwpx` / `.pdf` | 시범사업 캐노니컬 (12쪽) |
| `...\세부사업 계획서\배전계통_운영권한과_읍면단위_DSO_20260910_v2.hwpx` / `.pdf` | **DSO 제2판** (6쪽, 본문·표 12pt) |
| `C:\Users\User\sakyowon-wiki\content\<분류>\` | 사교원 위키 이전분 14건 |
| `skills\hwpx-powershell-edit\scripts\patch_body_12pt.py` | 12pt 사본 생성 · 교정본 확정(clear_red) |

## 약속

- 사용자가 **"이어서 작업하자"** 라고 하면 이 핸드오버의 "미완료" 목록부터 재개
- 사용자가 **"정리해"** 라고 하면: ①핸드오버 ②스킬 ③레슨 ④위키배포 자동 수행
