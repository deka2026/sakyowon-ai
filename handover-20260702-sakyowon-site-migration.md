# 핸드오버: Claude Code 조직 권한 정책 초안 + 사교원 사이트 진짜 레포 정정

**날짜**: 2026-07-02
**세션**: Claude Code 관리형 설정 문의 + 사교원 우편함 확인

---

## 배경

한 세션에서 서로 다른 두 가지 일이 진행됨:
1. Anthropic 콘솔의 "관리형 설정(Managed Settings)" 기능 문의에서 시작해, 사교원 조직 직원 3단계 권한 등급(최상위/중간관리/일반) 정책 초안 작성.
2. "우편함 봐줘" 요청 처리 중, 사교원 사이트의 실제 작업 레포가 기존에 알던 것과 다르다는 정정을 받음.

## 1. Claude Code 조직 권한 등급 정책

- 조사 결과: Anthropic 콘솔의 관리형 설정은 **조직 전체에 단일 정책만 적용**되고, 사용자·그룹별 차등 배포 기능은 지원하지 않음 (Anthropic 공식 문서 확인).
- 대안 설계: **①전 직원 공통 baseline(`deny`만, 콘솔 관리형 설정에 등록)** + **②등급별 폴더·레포 접근권한 분리**의 이원 구조로 3단계 구현.
  - Tier 1(최상위, 김일영): 전체 명령·읽기·쓰기 허용, 본인 전용 레포/폴더에 배치
  - Tier 2(중간관리, 김민수·이주현): 담당 프로젝트 폴더 한정, 경영전략·인사·재무 폴더 접근 차단
  - Tier 3(일반, 나머지 직원): 본인 업무 폴더만, plan 모드 기본값
- Artifact 문서(다운로드 가능한 4개 settings.json 포함): https://claude.ai/code/artifact/77d2eba5-4026-44ae-8809-1e1dd4b94be2
- **핵심 제약**: Claude Code는 사용자 신원을 구분하는 개념이 없으므로, JSON의 `deny`만으로는 등급 격리가 되지 않음. 실제 격리는 GitHub 레포 권한 또는 OS 폴더 권한이 선행되어야 함.

## 2. 사교원 사이트 진짜 레포 정정

- 기존에 쓰던 경로(`haeory-cyber/poomasi-site`의 `sakyowon/` 하위 폴더, fork+PR 방식)는 **착오였음**. 실제 작업 레포는 독립 저장소 **`haeory-cyber/sakyowon-site`**.
- 2026-07-02 김일영이 이 레포에 접근 권한을 받아 로컬에 새로 클론: `C:\Users\Admin\haeory-sakyowon-site\`
- ⚠️ 주의: `C:\Users\Admin\sakyowon-site\`(경로 유사)는 전혀 무관한 옛 저장소(`deka2026/sakyowon-site`, 정적 1페이지)이므로 혼동 금지.
- 우편함은 이 레포 루트의 `JIMMY-DECA.md` 하나만 사용. poomasi-site 쪽 옛 우편함은 폐기.
- 레포의 `CLAUDE.md`/`MANAGER.md` 확인 완료: 역할 분담(데카=김일영의 AI=프론트 담당, 지미=품앗이 본부 AI=백엔드/서버 담당), 권한 범위, `/api/inquiries`·`/api/ai`·`poomai-widget.js` 호출 코드는 절대 변경 금지.
- 지미가 남긴 메시지 4건 확인 (전부 2026-07-01):
  1. 14:20 — 역할 소개 + 부탁 3가지(CLAUDE.md 준수, `/api` 호출 코드 유지, 막히면 우편함에 남기고 추측 금지)
  2. 18:32 — `academy-site_자동화_온보딩_기획안.md` 전달(academy-site를 공식 온보딩 플랫폼으로 확장하는 초안)
  3. 19:03 — ⚠️ 위 기획안 **보류** 통지
  4. 19:35 — 위 기획안 **폐기**, 새 방향 **"3+얇은1" 구조** 확정(학습자 자기 Claude Code로 체크리스트 실행, 로그인은 기존 연대지능 회원 시스템 재사용, 강사·이사장용 얇은 진도 대시보드, 챗봇은 폐기). **지금은 김일영이 직접 검토하는 단계 — academy-site를 임의로 손대지 말 것.**

## 3. 지미 답장 감시 체계

- 김일영이 지미의 요구사항을 반영해 우편함에 답장을 push할 예정 → 지미의 새 회신 대기 중.
- 세션 내 동적 루프(20~30분 간격)로 시작했다가, "1시간 간격" 요청을 받아 **클라우드 스케줄로 전환**.
- 작업명: `sakyowon-mailbox-watch` / cron `0 * * * *`(매시 3분) / 세션 종료 후에도 계속 동작.
- 새 "지미 → 데카" 항목 발견 시 스스로 요약해 알리고 작업을 비활성화하도록 프롬프트에 내장.
- 세션 종료 시점(2026-07-02) 기준: 아직 지미 답장 없음. 마지막 항목은 여전히 2026-07-01 19:35 메시지.

## 결정 사항

- Claude Code 관리형 설정만으로는 사람별 권한 차등이 불가능 → 폴더/레포 접근권한 분리가 필수 전제.
- 사교원 사이트 작업은 앞으로 전부 `haeory-cyber/sakyowon-site` 기준으로 진행. poomasi-site 경로는 폐기.
- academy-site는 지미의 "3+얇은1" 새 방향 상세 자료가 도착하기 전까지 임의로 수정하지 않음.

## 미완료 / 확인 필요

- [ ] `haeory-cyber/poomasi-site#1` PR(옛 경로 기준으로 올렸던 것) — 닫을지, 새 레포 기준으로 재작업할지 김일영 지시 대기 중.
- [ ] 권한 등급 JSON 4종을 실제 콘솔/레포에 배포하는 작업은 아직 초안 단계, 실제 적용 안 함.
- [ ] 지미의 답장 대기 중(클라우드 스케줄이 자동 감시).
- [ ] academy-site "3+얇은1" 새 방향 상세 설계 자료 수신 대기.

## 파일 위치 안내

| 경로 | 설명 |
|------|------|
| `C:\Users\Admin\haeory-sakyowon-site\` | 사교원 사이트 진짜 작업 레포 (신규) |
| `C:\Users\Admin\poomasi-site\sakyowon\` | 옛 경로 (폐기, 더 이상 작업 안 함) |
| `C:\Users\Admin\.claude\projects\C--Users-Admin\memory\project-sakyowon-site.md` | 최신 레포 정보 메모리 |
| `C:\Users\Admin\.claude\scheduled-tasks\sakyowon-mailbox-watch\` | 우편함 감시 클라우드 작업 |
