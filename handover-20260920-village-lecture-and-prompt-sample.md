# 핸드오버: 마을강사단 강의자료 6질문 재구성 · PDF 링크 복구 · 완도 프롬프트 샘플

**날짜**: 2026-09-20
**이전 핸드오버**: handover-20260918-claude-config-sync.md
**작업 폴더**: `D:\사교원 개발그룹\사교원 개발그룹\연대지능활동가 아카데미`

---

## 수행한 작업

### 1. 강의안을 여섯 질문 목차로 재구성 (연대지능 위키 근거)

기존 20장(3부 구성)을 **여섯 질문** 목차로 새로 지었다. 내용 근거를 전부 위키에서 끌어와 각 장 각주에 원 문서명을 박았다.

- 자료원: `wiki.poomasi.org` contentIndex 378건, `sakyowon.co.kr/sakyowon-wiki` 108건을 내려받아 대조
- 핵심 인용 문서: 「도표 – 세 지능」, 「도표 – 폴라니의 네 허구상품」, 「도표 – AI 활용 6단계」, 「Domain AI」, 「하네스 엔지니어링」, 「모델은 부품, 데이터가 본체」, 「검증은 만든 AI가 아니라 다른 AI에게 시킨다」
- 기존 체계도 2장(AI 업무 체계도 · AI 하네스 구조도)은 PowerPoint COM `InsertFromFile`로 재삽입
- 부록 A 용어사전 3장 추가 → 26장

빌더: `build_강의PPT_20260918.py`

### 2. 수치 검산

강의에 쓰는 숫자를 원자료와 대조하고 직접 계산했다. **산식은 전부 일치.** 표기 3건을 바로잡았다.

| 항목 | 검산 |
|---|---|
| 하루 11.3시간 / 1인 2.26시간 | 678분 ÷ 60 = 11.3, 678 ÷ 5명 = 135.6분 = 2.26 ✓ |
| 매장당 연 4,124만 원 | 2.26 × 5 × 365 × 1만 = 4,124.5만 ✓ |
| 19매장 7.84억 | 4,124만 × 19 = 7.84억, 원자료 거시산출 6.9억과 오차 12% 이내 ✓ |

바로잡은 표기: ①"100쪽 발제문에서 102곳"→"1~3장 약 960문단에서 102곳" ②자동화 수치에 산출 시점(2026.5.) 명기 ③창업 서비스 실패율에 "창업자 인터뷰 인용값" 단서 추가(원자료가 미검증으로 명시)

### 3. 한글 교안 6질문판

`마을강사단_AI교육_교안_6질문_20260918.hwpx` (14쪽) — 빌더 `build_교안_20260918.py`
부록 B에 **수치 근거·검산표**를 문서로 남겼다. 템플릿 `tpl_all12.hwpx`가 폴더에 없어 이전 세션 임시폴더에서 찾아 복사해 두었다.

### 4. PDF 링크 복구 (이번 세션 최대 건)

사용자가 만든 `..._입문_20260918_링크추가.pdf`의 바로가기 링크가 눌리지 않았다. 원인과 처리가 두 겹이었다.

| 증상 | 원인 | 처리 |
|---|---|---|
| 링크가 있는데 안 눌림 | 주석 사각형이 글자보다 위로 밀려 겹침 1.3pt (높이 2.8pt) | 글자 **줄 bbox** 기준으로 사각형 재설정 → 높이 6.7~9.7pt |
| 쪽마다 둘째 링크 없음 | PowerPoint PDF 내보내기가 쪽당 하나를 통째로 누락 | PPTX rels에서 주소를 읽어 26건 복원 |
| 주소 대조 전패 | PDF 글자가 **NFD 분해 한글** | 양쪽 NFC 정규화 |
| 엉뚱한 주소가 붙음 | 순서 재배치 후에도 slideN.xml 파일번호는 그대로 — 쪽 번호로 쓰면 어긋남 | `presentation.xml` sldIdLst의 **발표 순서**로 매핑 |

결과: 링크 **149개 전부** 화면 주소와 실제 연결 주소 일치(불일치 0), 링크 없는 주소 글자 0건.

### 5. 강의안 순서 재배치 — 팀 바이브코딩을 맨 앞으로

Q5(팀) → Q1으로 올리고 나머지를 밀었다. 슬라이드 이동 + 제목 Q번호 18개 + 번호칩 18개 + 목차 6줄을 함께 갱신. 하이퍼링크 53개 유지 확인.

- PPT: `마을강사단_AI교육_입문_20260918_링크추가.pptx` (26장)
- 교안: `마을강사단_AI교육_교안_6질문_20260919.hwpx` — 빌더 `build_교안_20260919.py`
- 앞당겨져 설명이 비는 두 곳을 예고로 메움: 네 역할 → "Q2에서 배울", 규모·스택 → "Q5에서 자세히"

### 6. 완도마을학교 통합사이트 프롬프트 샘플 (신규)

권한 등급·관리자 기능·인스타 연동을 요구한 프롬프트 샘플을 만들고, 이어서 **완도 마을학교 통합사이트**로 전면 재작성했다. 망남 1차판은 사용자 지시로 삭제.

- 권한 **6등급**: 손님·학생·학부모·학교운영자·담당공무원·통합관리자
- 뼈대는 **칸막이 둘**: 학교 칸막이(운영자는 자기 학교만) / 공무원 칸막이(집계만, 읽기 전용)
- **만족도 조사**: 프로그램 종료 시 자동 발송, 학생용·학부모용 분리, 익명 처리, 응답률 50% 미만 시 1회 재알림
- **학교별 회계 자동 처리**: 영수증 초안화·참가비 대조·집행현황표 자동 생성, **자동 확정 금지·자동 이체 금지**(반자동 트리아지)
- 산출: PPT 20장 / md(복사용 원문) / hwpx. 원문은 PPT 빌더 `PAGES`가 정본이고 md·hwpx가 이를 읽어 찍는다

### 7. 한글판 가로 편집용지 · 14pt

사용자 요청으로 A4 **가로**(297×210) + 본문 **14pt** 판을 만들었다. 가로 템플릿 `tpl_all12_가로.hwpx`를 따로 만들어 세로 템플릿은 보존. 표 폭을 전부 다시 잡았고(라벨 칸이 접히지 않게), 프롬프트 본문은 **표 하나**로 합쳤다.

프롬프트 줄바꿈을 가로 폭에 맞춰 재흘림해 봤으나 한글이 한 번 더 접어 들여쓰기가 깨져 **되돌렸다**. 복사해 붙일 원문이라 줄바꿈 자체가 내용의 일부다.

### 8. 최종본 지정

사용자가 한글에서 직접 다듬은 **`프롬프트샘플_완도마을학교통합사이트_20260920_강의자료.hwpx`** 를 최종본으로 지정했다. 메모리에 기록.

### 9. 위키 배포 (정리해 루틴 ③④)

레슨 「AI와 함께 PDF 링크 되살리기」를 두 위키에 올렸다.

| 위키 | 결과 |
|---|---|
| **사교원 위키**(기본) | 1단계 push `210a1e8` → 2단계 CI 빌드 **success** (run 35489821658) → 3단계 미러 push `ed8f03f` (112→**113쪽**) → **4단계 서버 반영 대기** |
| 연대지능 공동위키 | PR **#33** 생성. **머지가 권한 정책으로 차단** — 사용자 승인 필요 |

라이브 확인(2026-09-20): `sakyowon.co.kr/sakyowon-wiki` sitemap 112개, 새 문서 404.
Last-Modified 09-19 01:59 — 서버가 아직 갱신되지 않았다. 4단계 명령이 남았다.

```bash
bash /opt/sakyowon/src/deploy-www.sh      # 서버에서 실행 (SSH 필요)
```


---

## 미완료 / 다음 할 일

- [ ] **프롬프트 샘플을 교안에 편입** — 사용자가 "교안에 넣을 것"이라 예고. 부록으로 붙일지 Q1 안의 절로 넣을지 미정. 넣을 때 슬라이드 참조 번호도 함께 재매김
- [ ] **교안 한글 렌더 확인** — `..._교안_6질문_20260919.hwpx`의 쪽수·표 넘침을 눈으로 본 확인이 미완. (세션 중 한글 COM이 멈춰 프로세스 정리함. 지금은 정상)
- [ ] **사교원 위키 4단계 서버 반영** — 3단계까지 완료. 서버에서 `bash /opt/sakyowon/src/deploy-www.sh` 실행 후
      `curl -sS https://sakyowon.co.kr/sakyowon-wiki/sitemap.xml | grep -c "<loc>"` 가 113이 되는지 확인
- [ ] **공동위키 PR #33 머지** — 권한 정책(Merge Without Review)으로 차단됨. 사용자가 승인하거나 직접 머지
- [ ] **구판 정리** — 강의안 4종·교안 2종·임시 스크립트 `_reorder_gyoan.py`가 남아 있다. 교안 편입 작업이 끝난 뒤 한꺼번에 정리하는 편이 나음
- [ ] **PPTX와 PDF가 어긋나 있다 (2026-09-20 발견)** — 사용자가 PPTX를 25장으로 편집했다(Q1-③ 프롬프트·시연 장 삭제,
      Q1-② 제목에 "(실제로 해보자)" 추가). 링크 PDF는 그 전 26쪽판이라 **내용이 다르다.**
      배포 전 PDF를 다시 뽑고 `pdf-link-repair`로 링크를 복구해야 한다
- [ ] **PDF 재생성 시 주의** — `..._링크추가.pptx`에서 PDF를 새로 뽑으면 링크가 또 깨진다. 새 스킬 `pdf-link-repair`로 복구할 것
      (2026-09-20 스모크 테스트: 사각형 186건 보정 + 누락 16건 복원, 주소 일치 202건)

---

## 파일 위치

| 경로 | 내용 |
|---|---|
| `D:\사교원 개발그룹\사교원 개발그룹\연대지능활동가 아카데미\` | 아래 전부의 작업 폴더 |
| `마을강사단_AI교육_입문_20260918_링크추가.pptx` | **강의안 최신** 25장(사용자 편집), 팀 먼저, 하이퍼링크 |
| `마을강사단_AI교육_입문_20260918_링크추가.pdf` | 링크 활성 PDF 149개 — **26쪽판이라 위 PPTX와 불일치** |
| `마을강사단_AI교육_교안_6질문_20260919.hwpx` | **교안 최신** 팀 먼저 |
| `프롬프트샘플_완도마을학교통합사이트_20260920_강의자료.hwpx` | **프롬프트 샘플 최종본**(사용자 편집) |
| `프롬프트샘플_완도마을학교통합사이트_20260919.pptx` / `.md` / `.hwpx` | 같은 내용의 PPT·원문·한글 이전판 |
| `build_강의PPT_20260918.py` · `build_교안_20260919.py` | 강의안·교안 빌더 |
| `build_프롬프트샘플_완도_20260919.py` · `..._hwpx_20260919.py` | 프롬프트 샘플 빌더(PPT가 원문 정본) |
| `tpl_all12.hwpx` · `tpl_all12_가로.hwpx` | 한글 템플릿 세로 12pt / 가로 14pt |

---

## 약속

- 사용자가 **"이어서 작업하자"** 라고 하면 이 핸드오버의 "미완료" 목록부터 재개
- 사용자가 **"정리해"** 라고 하면: ①핸드오버 ②스킬 ③레슨 ④위키배포 자동 수행
