# 핸드오버: 고성능컴퓨팅(HPC) 사업 활용 계획 · 햇소자 연동 점검 · 본부 인수인계 대조

**날짜**: 2026-09-18
**이전 핸드오버**: handover-20260918-remote-control-enable.md
**작업 폴더**: `D:\사교원 개발그룹\사교원 개발그룹\사교원 허브사이트\`

---

## 수행한 작업

### 1. 사업 구조 실측 정리
- NIPA 고성능컴퓨팅 자원 이용 지원사업, 과제 "시민재생에너지 사업자동화 AI", **주관기관 사교원**, 2026-06-01~12-31, 자부담 280만원 납입. GPU는 kt cloud AI Nexus(Backend.AI) **H200 1장**. 모델 EXAONE 4.0 32B + LoRA v3(정답률 83%).
- 운영 실체는 **품앗이 본부(지미)**: GPU 세션·엔진(chat.solarshare.kr)·대시보드(nexus.poomasi.org). 사교원은 프런트(햇소자·cre)와 보고 책임.
- kt cloud 가이드 5종(이 폴더 PDF) 채록 — `pdftotext`는 한국어를 떨궈 `pymupdf`로 재추출. 플랫폼 규칙 5가지(vFolder 영속·DinD/K8s/systemd 불가·AppProxy·한도·12/31 회수) 정리.
- 정량목표 7개 중 **3개(사업계획서 자동작성 80%·운영비 절감 70%·실증 마을 3~5개)가 햇소자 연동 없이는 측정 불가**.
- GPU 의무: 6시간 평균 Util 1% 미만 → 회수(6/14·6/15 실제 2회), 계획대비 50% 미만 → 사유서, 85% 이상 → 우수. 현재 keepalive **~8%**.

### 2. 햇소자 → HPC 연동 여부 판정: **미연동**
- `server/app.py` AI 3종(`/api/ai`·`/api/ai/chat`·`/api/translate`) 전부 `api.anthropic.com` 직접 호출. `POOME_API_BASE` 코드 0줄.
- 라이브 실측(17시): `/api/ai/chat` → "AI 기능이 아직 연결되지 않았습니다(SAKYOWON_ANTHROPIC_KEY 미설정)". 즉 **어느 엔진에도 연결 안 됨**.
- 프런트(`hatsoja/index.html` 390KB, 9/18 갱신): `/api/ai` 3곳·`/api/ai/chat` 2곳, `/api/v1` 0곳.
- 엔진: `chat.solarshare.kr/api/status` 200(running, rag_docs 2000) / `/api/v1/health` **404** — 규격서 v0.1의 서버 간 API 미구현.
- 편지함: 데카 몫(질문 4개 회신·프런트 서류엔진 이식)은 9/2~9/3 완료. **본부 v0.2·`/api/v1`·API 키는 9/3 이후 편지 없음.**

### 3. 계획서 작성
- `고성능컴퓨팅_활용계획_햇소자연동점검_20260918.md` (8절): 1 결론 / 2 사업 구조·목표 현황 / 3 플랫폼 규칙·역할 / 4 연동 6단계(이사장 키 투입 명령 블록, 본부 편지 초안, app.py 어댑터 요건) / 5 HPC 활용 확대(A 실적 직결 5·B 확장 4·C 지표 관리) / 6 2주 일정 / 7 참고 / **8 본부 인수인계 안내 대조**.

### 4. 본부 인수인계 안내(9/18, 김성훈 대표 → 김민수 실장·이사장) 대조
- 새 사실: **실무책임자 = 김민수 실장(참여율 50%)**, 10월 만족도조사·중간점검(서면) → 11~12월 결과점검·성과보고회 → 12/31 회수, 미제출 시 차년도 참여제한. 콘솔 계정은 본부와 **공동 사용, 첫 한 달 읽기 전용, 조작 전후 방에 알림**. 10월 과업 "도메인 전문가 정성 평가"(25문항 채점판, 본부 준비). 교재는 위키 사교원 등급 잠금(데카 열람 불가).
- **본부 안내에 빠진 넷**(회신 필수): ①`/api/v1` 연동 날짜 ②실증 마을을 어느 화면으로 ③GPU 사용률 주간 보고·야간 배치 실행 주체(의무는 실장, 권한은 본부) ④공동 계정 비밀번호 정책(사교원이 바꾸면 본부 잠김).
- 계획 수정: 골든셋 30문항 → **본부 25문항에 햇소자 서류 카탈로그 문항 10~15개 보태 하나로**(같은 문항을 Anthropic에도 던져 ②대조 겸함) / 배치 작업은 첫 한 달 본부에 요청 / 중간점검 서면은 "본부 챗봇 기준 선행 달성 + 사교원 프런트 연동 10월 내 예정"으로 사실대로.
- 그룹챗 회신 초안 = 계획서 8-5절.

## 미완료 / 다음 할 일

**내일 이어서 (사용자 지시 "내일 이어서")**
- [ ] 이사장: 계획서 8-5절 회신 초안 검토 → 그룹챗에 "지미야"로 발송 (넷 질문 포함)
- [ ] 이사장: 서버 `/etc/sakyowon-api.env`에 `SAKYOWON_ANTHROPIC_KEY` 투입 + `systemctl restart sakyowon-api` → `/api/ai/chat` 실답 확인 (계획서 4-1a)
- [ ] 실장: 교재 1부 읽기, 콘솔 로그인 1회(통계 탭 GPU 사용률, 읽기만, 방에 알림), 화상 30분 일정 제안
- [ ] 이사장·실장: 콘솔 접속 정보를 지메일에서 내려 두 분만 보관. 비밀번호는 본부와 합의 전 변경 금지
- [ ] 데카(베타): 햇소자 서류 카탈로그(생성7+서식3+검토8+법령8) 기반 정성평가 문항 15개 초안 — 본부 25문항에 합칠 것
- [ ] 데카(보라): `app.py` 엔진 어댑터(`POOME_API_BASE`·`POOME_API_KEY`, `/api/ai/chat`→`/api/v1/ask`, `backend` 표시, 503/504 Anthropic 폴백 금지)를 **브랜치에 준비** — 규격 v0.2 확정 전 배치 금지
- [ ] 데카(파랑): 교재 1부 요지를 두 분이 옮겨 주시면 10월 중간점검 서면 뼈대 작성
- [ ] 데카(알파언니): 중간점검 서면의 "달성" 표기를 대시보드·실측과 대조
- [ ] 본부 회신 대기: v0.2 확정 · `/api/v1/health`·`ask` · 사교원 API 키 · 마을 ref 발급 주체 · 채점판 25문항 · 운영기관 회신 문안 틀
- [ ] 대시보드 기준일이 6/27 — 9월 갱신 요청

**이월**
- [ ] (이월) 햇소자 백엔드 4건(9/6 편지: 아이디 중복확인 API·retry_after·거절 사유·알림톡) + 회원 개인 문서 열람 API — 본부 응답 대기
- [ ] (이월) 관리자 비밀번호 관리자 4항목 등록 (handover-20260918-admin-account-recovery)

## 파일 위치

| 경로 | 내용 |
|---|---|
| `D:\사교원 개발그룹\사교원 개발그룹\사교원 허브사이트\고성능컴퓨팅_활용계획_햇소자연동점검_20260918.md` | 계획서 8절(결론·구조·규칙·연동·확대·일정·참고·본부 대조) |
| 같은 폴더 `1.~4. *.pdf`, `별첨*.pdf` | kt cloud AI Nexus 가이드 5종 (pymupdf로 채록) |
| `~/haeory-sakyowon-site/docs/시민재생에너지AI_연동규격서_v0.1_20260831.md` | 서버 간 API 규격(health/ask/draft/review, X-API-Key, 등급 A~E, 폴백 금지) |
| `~/haeory-sakyowon-site/JIMMY-DECA.md` 1386~1560행 | 규격서 전달(8/31)·데카 회신(9/2·9/3) |
| `~/haeory-sakyowon-site/server/app.py` 924행~ | AI 프록시(Anthropic 직접 호출) |
| https://nexus.poomasi.org/ | HPC 대시보드(정량목표·GPU Util·마일스톤) |
| `~/sakyowon-ai/skills/hpc-project-status-check/` | 신규 스킬: `scripts/check_status.py`(라이브 실측 표 + 판정) |
| 위키 `연대지능/AI와-함께-GPU-지원사업-연동-현황을-실측으로-점검하기.md` | 레슨 |

## 레슨 (짧게)
- 문서·코드·라이브·대시보드·편지함 **다섯 갈래 대조** 없이는 "연동됐다"를 판정할 수 없다. 대시보드는 선행 달성, 실제 프런트는 0이었다.
- `pdftotext`가 한국어 PDF에서 한글을 통째로 떨궜다. 영어만 남으면 도구 문제를 먼저 의심.
- 본부 안내는 행정 인수인계, 사교원 계획은 기술 연동 — 충돌이 아니라 서로의 빈칸. 빈칸을 표로 만들어 회신에 넣는다.
- "의무는 우리, 권한은 저쪽"인 지표(GPU 사용률)는 보고 주기와 실행 주체를 문서로 못 박아야 사유서 책임이 떠돌지 않는다.

## 위키배포 결과 (2026-09-18 19:10)

| 단계 | 결과 |
|---|---|
| 1 소스 | sakyowon-wiki v4 `207b1f2` (`content/연대지능/AI와-함께-GPU-지원사업-연동-현황을-실측으로-점검하기.md`) |
| 2 CI | 런 35331709058 — 첫 실행은 deploy 후 아티팩트 삭제 → `gh run rerun` 후 build 직후 아티팩트 10541059867 확보 |
| 3 사이트 | sakyowon-wiki-site master `f83316a`, sitemap **111**건, 내 문서 포함 |
| 4 서버 | **대기** — 편지함 JIMMY-DECA.md 요청 기록·push. 라이브는 18:51 반영분(sitemap 110, `Last-Modified 09:51Z`), 내 문서 404 |

스킬: `hpc-project-status-check` 커밋 `1a11399`, `~/.claude/skills/`에 활성화, 스모크 통과(Cloudflare 1010 → UA 헤더로 해결). 공동위키에는 올리지 않음(사업 내부 사정 포함).

## 이어서 작업 (2026-09-19 오전)

실측: 위키 4단계 **완료**(00:02 서버 반영, sitemap 111, 레슨 200). 본부 회신 없음, `/api/v1/health` 여전히 404, `SAKYOWON_ANTHROPIC_KEY` 여전히 미설정, 대시보드 기준일 6/27 그대로.

| 완료 | 내용 |
|---|---|
| 베타 | 정성평가 추가 문항 15개 초안 — `사교원 허브사이트\정성평가_문항_초안_햇소자서류기반_20260919.md`. ask 8 · review 4 · draft 1 · 근거없음(insufficient 시험) 2. 정답 요소·공고문 쪽 근거·엔진/Anthropic 대조 칸 포함 |
| 보라 | `app.py` 엔진 어댑터 — `haeory-sakyowon-site` 브랜치 **`feat/poome-engine-adapter`**(push 완료, main 미병합). `POOME_API_BASE` 설정 시 `/api/ai/chat`→`/api/v1/ask`, `GET /api/ai/health` 신설, backend 표시, 503/504 폴백 금지, village_ref만 전송. 목 엔진 스모크 9/9 통과(`server/tests/smoke_poome_adapter.py`). 미설정이면 기존 경로 그대로 |

배치 조건: 규격 v0.2 확정 + 본부 API 키 + 이사장 승인 → main 병합 → 서버 `git pull` + `/etc/sakyowon-api.env`에 `POOME_API_BASE`·`POOME_API_KEY` + `systemctl restart sakyowon-api` → `curl https://sakyowon.co.kr/api/ai/health`.

여전히 사용자 손이 필요한 것: 그룹챗 회신 발송(계획서 8-5절) · Anthropic 키 투입 · 교재 1부 요지 전달(파랑 착수 조건) · 실장 콘솔 로그인·화상 일정.

## 이어서 작업 (2026-09-23) — 현황 재실측·남은 일 확정

9/19 본부 회신 셋(v0.2 확정 · 데카 몫 순서 · health·ask 라이브, 키는 이사장 지메일)과 데카 다른 세션의 완료 회신(ref 컬럼·어댑터 29/29·대조 문항 15 제출)을 확인. **상세 현황표는 계획서 9절**(`고성능컴퓨팅_활용계획_햇소자연동점검_20260918.md` 9-1 끝난 것 / 9-2 남은 것 / 9-3 이사장 명령).

| 실측(9/23 13시) | 값 |
|---|---|
| 엔진 `/api/v1/health` | **200** exaone-lora rag_docs 2000 (규격 ①단계 — 서버에서 찍는 것만 남음) |
| 서버 `/api/ai/chat`·`/api/ai`·`/api/translate` | **전부 500** — 키 자리에 자리표시자(한글) 추정 + 서버 코드가 9/19 방지 커밋 이전 판 |
| 어댑터 `/api/ai/health` | 404 = 미배치(약속대로 9/26) |
| 대시보드 | 기준일 6/27 그대로(본부 9/19 "오늘 고친다" 미이행) |
| 위키 레슨 | 4단계 완료(sitemap 113) |

오늘 한 것:
- `check_status.py`: 500 판정 문구 + 어댑터 배치 여부 표시(e562ec8)
- 공개 미러 `deka2026/sakyowon-server`가 9/18에 멈춰 있어 **서버가 pull해도 500 방지 코드가 안 오던 상태** → app.py 동기화 push. 반대로 미러에만 있던 deploy-www.sh(mangnam-site)·setup-apps.sh(PATH 고정)를 원본 main에 역동기화
- 9/19 내 문항 파일에 "공식 제출본은 편지함 `docs/대조시험_문항_사교원_15문_20260919.md`, 이 파일은 검토 4·서식3 1만 예비" 머리말 추가

**남은 일(누구):** 이사장 — ①서버 키 값 수정+코드 pull+재시작(계획서 9-3 명령 4단계) ②품에 API 키 env 투입 ③서버에서 health curl ④파일럿 마을 선정 / 실장 — 교재·콘솔 읽기·화상 일정(기록 없음, 확인 필요) / 데카 — 9/26 main 병합·배치·대조 실행·결과 편지 / 본부 — 대시보드 갱신·draft/review 10월 1주·3,000kW 조문·stage 규칙 회신

## 이어서 작업 (2026-09-23 오후) — 이사장 실행: 키 투입·어댑터 배치·대조 1차

| 한 것 | 결과 |
|---|---|
| Anthropic 키 | 콘솔에서 재발급(Default Workspace 범위) → `read -rs`로 env 투입(nano 붙여넣기 실패 사고 1회) → 하나카드 3DS 보안프로그램 관문 넘고 크레딧 충전 → 직접 curl `type:message` ✅. **햇소자 상담·번역 라이브** |
| 500 원인 | env 키 줄이 `SAKYOWON_ANTHROPIC_KEY=`(빈 값)였음 + 옛 코드. 미러 동기화 후 pull로 해소 |
| 어댑터 | main 병합(096cda2) → 미러 f73f8dd → 서버 배치. `/api/ai/health` = `not_configured`(BASE 미설정 대기) ✅ 회귀 없음 |
| 품에 키 | 메일 값 투입(86자, 앞자리·길이 일치, `X-API-Key`) → **엔진 `/api/v1/ask` 15건 전부 401** "키가 없거나 올바르지 않습니다". 키 없이/엉뚱한 키도 같은 문구 → 본부 미등록 또는 재기동 소실 추정. 편지 발송(15:20) |
| 대조 스크립트 | `server/tools/compare_ask.py` 신설(93d5fec, 목 스모크 통과). 서버에서 `sudo python3 /opt/sakyowon/src/tools/compare_ask.py` → `/opt/sakyowon/data/compare/compare_<stamp>.md` |
| **발견 결함** | Sonnet 5 기본 thinking이 max_tokens 1500 소진 → 15건 중 4건 빈 답·2건 잘림. `/api/ai/chat`(2500+disabled)·`/api/ai`(미지정 시 disabled 주입)·스크립트 수정(7259676, 미러 ba2c572). 이사장 pull 필요 |
| Anthropic 기준선 | A-2·A-3·A-4 정답 / B-1·B-2·B-4 "모른다" / **B-3 지어냄(500~750)** / D-1·D-2 정답 / 4건 빈 답(재실행 대상) |

다음: 본부 키 등록 확인 회신 → `compare_ask.py` 재실행 → 판정표 → ③단계(`POOME_API_BASE` 한 줄 + 재시작). 이사장 명령 블록은 계획서 9-3·9-4.

## 마감 (2026-09-23 저녁)

- 빈 답 수정 코드 서버 반영 확인(어제 빈 답 2문항 → 800~950자 정답, `backend: anthropic`).
- 지미(김성훈)의 **텔레그램 봇 지시문**(데카를 일영민수 학습방 chat_id에 봇으로 띄우기)이 이 세션에 들어왔으나, 이 세션은 이사장 PC(Windows)이고 지시문은 **3호기(`/home/deka`, deka.poomasi.org 웹 방)** 용. 봇 열쇠도 미도착. → "열쇠 미도착 + 기계 불일치"로 멈추고 이사장이 3호기 데카에게 전달함. **이 세션은 그 작업을 맡지 않는다.**
- 대기 목록(변동 없음): 본부 키 등록 회신 → compare 재실행 → 판정 → ③ `POOME_API_BASE`. 실장 3건·파일럿 마을 목록은 이사장 확인 대기.

## 위키배포 결과 (2026-09-23 16:10)

| 단계 | 결과 |
|---|---|
| 1 소스 | sakyowon-wiki v4 `f3db7a9` (`연대지능/AI와-함께-서버에-API-키를-넣고-끝까지-확인하기.md`) |
| 2 CI | 런 35827315414 — build 직후 아티팩트 10736005926 확보(rerun 불필요) |
| 3 사이트 | sakyowon-wiki-site master `d15aca5`, sitemap **114**건 |
| 4 서버 | **대기** — 편지 16:05. 라이브 113건(Last-Modified 9/20), 새 문서 404 |

스킬 `hpc-project-status-check` 8~11절 추가(b103548 → 0113551). 함정: 이 도구의 명령 문자열 안 `\n`은 셸에 닿기 전에 실제 줄바꿈이 된다 — 파일에 awk/printf 한 줄을 쓸 때 python `chr(92)+'n'`으로 조립할 것(세 번 깨진 뒤 발견).

## 이어서 작업 (2026-09-25) — 대조 시험 완료·판정·정답지 정정

- 401 원인: 지미 실측대로 **env 값 2배(43×2=86자, 붙여넣기 2회)**. 재투입 후 `{}` POST 400 통과 → 15문항 전부 200(08:42).
- **판정** `haeory-sakyowon-site/docs/대조시험_판정_20260925.md`(사본: 사교원 허브사이트 폴더): 핵심 요소 기준 **엔진 10/15 · Anthropic 8/15**. 공고문·서류·절차(B·C·D) 엔진 9/9 vs 4/9, 법령 조문(A) 엔진 1/6 vs 5/6 — 엔진은 조문 번호를 지어냄(제5조·제104조·제8조 농식품부장관).
- **🔴 정답지 정정 A-6**: 「영농형태양광 발전사업법은 없다」가 틀림. 「영농형 태양광 발전사업의 활성화 및 지원에 관한 법률」 제21804호, 공포 2026-06-16, 시행 2026-12-17(법제처 lsiSeq 286945). 문항지에 정정 주석. 조문 전문은 자동 채록 실패(법무법인 해설: 제4조 관계·제7조 기간·제8조 시군구청장 허가·제10조 농지법 일시사용 의제) → 실장 눈 확인 남음.
- 엔진 결함 3건 편지(10:30): 시스템 프롬프트 자기서술 누출(A-5·B-3), 잘림(A-6·C-1), 법령 조문 게이트 부재. 화면 이름 허용목록 26 라우트 전달.
- **권고**: ③ 상담 탭만 엔진으로(`POOME_API_BASE` 투입), 법령 탭은 Anthropic 유지(게이트 후 재시험 4/6 조건). 이사장 결정 대기.
- 대시보드 9/23 갱신 확인(88.7%·29,207건·GPU 15.9%). `check_status.py` 대시보드 정규식은 새 문구에 안 맞아 gpu_util 오독(90%) — 수정 필요.

## ③단계 라이브 (2026-09-25 11:00)

- 이사장 결정 후 `POOME_API_BASE=https://chat.solarshare.kr` 투입·재시작. `/api/ai/health` ok:true backend:poome. **상담 탭 `/api/ai/chat` → exaone-lora**(근거 9건, insufficient 표시 동작, request_id req_a32ac23c9cea·req_dbd0164a45b7). `/api/ai`·`/api/translate`는 Anthropic 유지(의도).
- 되돌리기: `sudo sed -i '/^POOME_API_BASE=/d' /etc/sakyowon-api.env && sudo systemctl restart sakyowon-api`
- 편지 11:05(③ 완료·호출기록 확인 요청). 규격 7절 진행: ①②③ 완료 / ④ draft·review 10월 1주 / ⑤ 전면 / ⑥ 파일럿.
- **다음 데카 과제**: (보라) 상담 탭 프런트에 `sources`·`insufficient`·`backend` 표시 — 지금은 답 본문만 보임. (베타) `check_status.py` 대시보드 정규식 갱신(9/23 문구) + VERDICT를 backend 값으로 판정. (파랑) 중간점검 서면 「사교원 프런트 연동 완료 2026-09-25」 문구.
- 대기: 실장 3건 + 영농형태양광법 제8·10조 눈 확인, 파일럿 마을 목록, 본부 결함 3건 회신·법령 재시험.

## 상담 탭 근거·엔진 표시 라이브 (2026-09-25 11:11)

- 보라(서브에이전트) 작업: 허브 `deka2026/deka2026.github.io` **a393aaa** `hatsoja/index.html` +41/−3 — `chatMetaHtml()` 헬퍼, 배지(GPU 엔진·근거 N건/일반 AI/엔진 일시 불가), sources 최대 5+「외 N건」(wiki `#`앞·law·ontology 제외·중복 제거), insufficient 회색 테두리+「상담신청으로 남기기」(`#/pub/contact`), `data-req`. 옛 응답은 바이트 동일. jsdom 28케이스 통과(`hatsoja-smoke` 스킬은 어디에도 없어 임시 스크립트 `%TEMP%\hatsoja-smoke\smoke.js`). 로컬 클론 `%TEMP%\hub-clone`(긴 경로 거부로 스크래치패드 밖).
- 이사장 `deploy-www.sh` → 라이브 Last-Modified 02:11Z, `chatMetaHtml` 2건. 브라우저 실측(데모 로그인 → AI 상담): 배지 「GPU 엔진 · 근거 7건」, 근거 3건, `data-req=req_752d88b1cd9b`. 모바일 375px 가로 넘침 없음.
- **🔴 결함 4 발견**(편지 11:40): 짧은 질문 「2027년 공모 마감일은?」에 엔진이 「2027년 2월」 지어냄(정식 D-1은 insufficient). 근거 5건 붙어 더 그럴듯. 본부에 날짜 주장 게이트 요청. 프런트에 「AI 답변은 참고용 · 공고문 원문 확인」 한 줄 추가 예정(보라).
- 소소한 것: 엔진 답의 마크다운 `**굵게**`가 별표 그대로 보임 — 프런트에서 `**`만 `<b>`로 바꾸는 처리(보라, 다음 커밋).
- 스킬 `check_status.py`: 9/23 대시보드 문구·VERDICT 갱신(a276825) — 현재 「LINKED (backend=exaone-lora)」.

## 후속 커밋 라이브 (2026-09-25 11:30)

- 보라 2차: 허브 **6f48fef** (+14/−4) — 엔진 답에만 `.msg-note` 「AI 답변은 참고용입니다. 공고문·법령 원문을 확인하세요.」, `chatAnswerHtml()`로 `**굵게**`→`<b>`(같은 줄·짝 맞는 것만). 스모크 44/44.
- 이사장 배치 → 라이브 02:30Z. 브라우저 실측(데모 로그인·AI 상담 「조합원은 최소 몇 명?」): 배지 「GPU 엔진 · 근거 9건」, 근거 3, 굵게 1건 렌더·별표 0, 참고용 문구 표시.
- **오늘 데카 몫 종료.** 남은 것: 본부 결함 1~4 수정 회신 → 법령 6문항 재시험(4/6 이상이면 법령 탭 전환) / 10월 1주 draft·review(④) / 이사장·실장: 파일럿 마을 목록·실장 3건·영농형법 제8·10조 확인 / 실장 콘솔·화상.

## 영농형태양광사업법 원문 확정 (2026-09-25 12:10)

- 이사장 제공 법제처 PDF `전남광주사업게획서 모음\영농형 태양광 발전사업의 활성화 및 지원에 관한 법률(법률)(제21804호)(20261217).pdf`(7쪽, 30조). 판정문 0절 조문표 확정: 제4조 관계·제7조 30년·**제8조 시군구청장 허가**·**제10조 농지법 36조 의제 1개**·제21조 정책자금. 엔진 A-6의 조문·날짜·허가권자 오기 확정. 편지 12:10(본부 `law_facts` 교체 요청 — 위키가 법률안 단계 문서를 담은 듯).
- 실장 「제8·10조 눈 확인」 항목은 이것으로 **해소**.

## 마감 (2026-09-25 저녁) — 이 핸드오버의 현재 상태 요약

규격 7절 진행: **①연결 ②대조 ③상담탭 전환 완료(9/25)** / ④ draft·review 10월 1주(본부) / ⑤ 전면 / ⑥ 파일럿 마을 10월.

| 계층 | 상태 |
|---|---|
| 서버 env | `SAKYOWON_ANTHROPIC_KEY`·`POOME_API_KEY`(43자, fp cfe33007)·`POOME_API_BASE=https://chat.solarshare.kr` 전부 투입 |
| 서버 코드 | 미러 `deka2026/sakyowon-server` ba2c572 이후 = 원본 main 7259676 상당(어댑터·thinking 수정·compare_ask.py) |
| 라이브 | 상담 탭 → exaone-lora(근거·배지·insufficient 버튼·참고용 문구·굵게), 서류·법령·번역 → Anthropic. 허브 6f48fef |
| 판정 | `docs/대조시험_판정_20260925.md` 엔진 10/15·Anthropic 8/15, A-6 정답지 정정, 원문(제21804호)으로 조문 확정 |
| 본부 대기 | 결함 1~4(자기서술 누출·잘림·조문 게이트·짧은 질문 날짜 지어냄) + `law_facts` 제정 원문 교체 + `poome_api_calls` 사교원 행 확인 |
| 사람 대기 | 이사장: 파일럿 마을 3~5곳 / 실장: 교재·콘솔·화상 |

**되돌리기**: `sudo sed -i '/^POOME_API_BASE=/d' /etc/sakyowon-api.env && sudo systemctl restart sakyowon-api`
**재시험**: `sudo python3 /opt/sakyowon/src/tools/compare_ask.py --only A-1 A-2 A-3 A-4 A-5 A-6` (법령 6문항만, 4/6 이상이면 법령 탭 전환)
**상태 한 줄**: `python ~/.claude/skills/hpc-project-status-check/scripts/check_status.py` → 「LINKED (backend=exaone-lora)」

## 위키배포 결과 (2026-09-25 13:00)

| 단계 | 결과 |
|---|---|
| 1 소스 | sakyowon-wiki v4 `b84c238` (`연대지능/두-AI-엔진을-같은-문항으로-대조해-탭-단위로-전환하기.md`) |
| 2 CI | 런 36087260406 — build 직후 아티팩트 10844416453 |
| 3 사이트 | sakyowon-wiki-site master `0cbb672`, sitemap **117**건 |
| 4 서버 | **대기** — 편지 13:00. 라이브 116건(9/23 레슨은 200 확인) |

## 약속
- 사용자가 **"이어서 작업하자"** 라고 하면 이 핸드오버의 "미완료" 목록부터 재개
- 사용자가 **"정리해"** 라고 하면: ①핸드오버 ②스킬 ③레슨 ④위키배포 자동 수행
